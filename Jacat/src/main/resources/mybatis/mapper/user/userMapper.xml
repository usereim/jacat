<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userMapper">
	<select id="selectUsersCntById" parameterType="String"
		resultType="int">
		select count(*) from users where id = #{id}
	</select>

	<select id="selectUsersCntByNick" parameterType="String"
		resultType="int">
		select count(*) from users where nick = #{nick}
	</select>

	<select id="selectUsersCntByEmail" parameterType="String"
		resultType="int">
		select count(*) from users where email = #{email}
	</select>

	<select id="selectUsersCntByIdEmail" parameterType="user"
		resultType="int">
		select count(*) from users where id = #{id} and email =
		#{email}
	</select>

	<insert id="insertUsersOne" parameterType="user">
		insert into users(id, pw, nick, email
		<choose>
			<when test="realFileName != null">
				, real_file_name, file_name, path, type)
			</when>
			<otherwise>
				)
			</otherwise>
		</choose>

		values(#{id}, #{pw}, #{nick}, #{email}
		<choose>
			<when test="realFileName != null">
				, #{realFileName}, #{fileName}, #{path}, #{type})
			</when>
			<otherwise>
				)
			</otherwise>
		</choose>
	</insert>

	<update id="updateUsers" parameterType="user">
		update users
		<set>
			<if test="nick != null">nick = #{nick},</if>
			<if test="email != null">email = #{email},</if>
		
			<if test="realFileName != null">real_file_name = #{realFileName},</if>
			<if test="realFileName == null">real_file_name = NULL,</if>
			
			<if test="fileName != null">file_name = #{fileName},</if>
			<if test="fileName == null">file_name = NULL,</if>
			
			<if test="path != null">path = #{path},</if>
			<if test="path == null">path = NULL,</if>
			
			<if test="type != null">type = #{type},</if>
			<if test="type == null">type = NULL,</if>
		</set>
		where id = #{id}
	</update>

	<select id="selectUsersOne" parameterType="user"
		resultType="user">
		select id, nick, grade from users where id = #{id} and pw =
		#{pw} and grade != 'S'
	</select>

	<select id="selectUsersIdByEmail" parameterType="String"
		resultType="String">
		select id from users where email = #{email}
	</select>

	<update id="updateUsersPwByEmail" parameterType="user">
		update users set
		pw = #{pw} where email = #{email}
	</update>

	<select id="selectUsersSuspendById" parameterType="String"
		resultType="int">
		select count(*)
		from users
		where id = #{id} and grade = 'S'
	</select>

	<select id="selectUsersByPw" parameterType="user"
		resultType="int">
		select count(*)
		from users
		where id = #{id} and pw = #{pw}
	</select>

	<select id="selectUsersOneForMypage" parameterType="user"
		resultType="user">
		select id, nick, email, real_file_name, file_name, path
		from users
		where id = #{id}
	</select>
</mapper>







