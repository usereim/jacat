<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="userMapper">
	<select id="selectUsersCntById" parameterType="String" resultType="int">
		select count(*) from users where id = #{id}
	</select>
	
	<select id="selectUsersCntByNick" parameterType="String" resultType="int">
		select count(*) from users where nick = #{nick}
	</select>
	
	<select id="selectUsersCntByEmail" parameterType="String" resultType="int">
		select count(*) from users where email = #{email}
	</select>
	
	<insert id="insertUsersOne" parameterType="user">
		insert into users(id, pw, nick, email
		<choose>
			<when test="realFileName != null">
				, real_file_name, file_name, path, type)
			</when>
			<otherwise>
			)
			</otherwise>
		</choose>
		
		values(#{id}, #{pw}, #{nick}, #{email}
		<choose>
			<when test="realFileName != null">
				, #{realFileName}, #{fileName}, #{path}, #{type})
			</when>
			<otherwise>
			)
			</otherwise>
		</choose>
		
	</insert>
</mapper>







